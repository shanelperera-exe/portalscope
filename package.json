{
  "name": "interdimensional-explorer",
  "version": "1.0.0",
  "private": true,
  "description": "Rick and Morty themed explorer used as a Harbor test workload",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev:frontend": "npm run dev -w frontend",
    "dev:backend": "npm run dev -w backend",
    "build": "npm run build -w backend && npm run build -w frontend",
    "lint": "npm run lint -w backend && npm run lint -w frontend",
    "format": "prettier --write \"backend/src/**/*.{ts,json}\" \"frontend/src/**/*.{ts,tsx,css}\" \"tests/**/*.ts\"",
    "test": "npm run test -w backend && npm run test -w frontend",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "prettier": "^3.6.2"
  },
  "engines": {
    "node": ">=20"
  }
}
